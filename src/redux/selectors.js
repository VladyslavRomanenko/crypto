export const selectStats = (state) => state.coins.coins.stats;
export const selectCoins = (state) => state.coins.coins.coins;
export const selectLoading = (state) => state.coins.loading;
export const selectNews = (state) => state.coins.news;
export const selectCoin = (state) => state.coins.coin;
